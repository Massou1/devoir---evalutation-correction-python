def pot ():
    val = pinl.read_digital():
    intensite = int(9*val.1023)
    retour intensite

    def colonned(P,Q):
        for i in range (S):
        display.set_pixel(P,y,Q)

        def aller (R,S)
        for i in range (R)
            colonned (i, S-R+I)

            while True : 

            for i in range (1,5)
            aller (i, intensite)
            slee(500)
            displayclear()